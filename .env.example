# Connection string for PostgreSQL (preferred)
# DATABASE_URL=postgres://username:password@hostname:5432/tech_finder

# Or provide discrete parameters instead of DATABASE_URL
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=tech_finder

# Enable this when your database requires SSL (e.g., managed cloud providers)
# PGSSLMODE=require
